"use client";

export function BottomUpIndicator() {
  return (
    <div className="rounded-lg bg-muted/40 p-3 text-xs text-muted-foreground">
      <strong className="text-foreground">Bottom-Up Approach:</strong> We always start with the
      foundation. Defensive planning supports every level above.
    </div>
  );
}

